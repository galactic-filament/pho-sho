db:
  image: ihsw/pho-sho-db

base:
  image: ihsw/pho-sho
  environment:
    - ENV=local
test-base:
  extends:
    service: base
  environment:
    - DATABASE_HOST=Db
  command: ./bin/run-tests

test:
  extends:
    service: test-base
  links:
    - db:Db
test-dev:
  extends:
    service: test-base
  volumes:
    - ./app:/srv/app
  links:
    - db:Db
